# from app.extensions import db

# # Neither of these are finished, still need to add ALL columns
# class User(db.Model):
#    puuid = db.Column(db.String(78), primary_key=True)
#    game_name = db.Column(db.String(16))
#    tag_line = db.Column(db.String(5))

# class Champion(db.Model):
#    id = db.Column(db.Integer, primary_key=True)